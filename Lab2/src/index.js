const {question,close} = require('./utils/readline');
const TeacherModel = require('./models/teacher-model');

/*0
.then( () => TeacherModel.list() )
.then( arr => arr.forEach((val)=>console.log(val)) );*/
//question("Insert text").then(a=>console.log("your answer: "+a)).then(close);